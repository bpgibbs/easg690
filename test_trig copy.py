import custom_trig as trg
# this should print 0, 1, 0
print(trg.sin(0), trg.cos(0), trg.tan(0))
# this should print 1, 0, inf (or something close to it)
print(trg.sin(pi/2), trg.cos(pi/2), trg.tan(pi/2))